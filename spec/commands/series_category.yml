

#
# Define data related command mixin with autogenerated field and method accessors
#
command_mixins:
    series_category:
        aliases: ["series_category_optional"]
        class: SeriesCategoryMixin
        mixins: ['source']
        meta:
            series_category:
                data: series_category
                relations: true

    series_categories:
        class: SeriesCategoryMultipleMixin
        mixins: ['source']
        meta:
            series_categories:
                data: series_category
                relations: true

#
# Define data related base command
#
command_base:
    series_category:
        mixins: ["series_category"]
        server_enabled: True
        groups_allowed: ['data-admin']

#
# Define data resource commands (list, get, save, remove, clear)
#
command:
    category:
        resource: series_category
        base: series_category
        base_name: series_category
        priority: 55